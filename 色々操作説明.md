# MV撮影モード 操作説明

この文書は、`feature/#XXX_MV`ブランチに追加したMV撮影機能と、ビルドでも使用できるデバッグショートカットの操作方法をまとめたものです。

## 起動方法

撮影機能をすぐ試す場合は、Unityで次のSceneを開いてPlayします。

```text
Assets/CreatorKousien/Scenes/Application/Boot.unity
```

`Boot`から次のSceneが自動的にAdditive Loadされます。

- `GameplayShell`
- 選択されたゲームプレイ用Stage
- `UI_HUD`
- `DebugOverlay`

撮影機能は`DebugOverlay`に配置されています。通常のゲーム進行を通して確認する場合は、`Title.unity`から開始しても利用できます。

MV撮影用にフィールドはワールドのXZ平面と平行な状態で固定され、ゲーム開始後も敵Waveは開始されません。

## 追加した撮影機能

### 撮影モード

撮影カメラ使用中は通常のカメラ制御を一時停止します。`F9`または`F6`で使用中のカメラモードを終了すると、次の状態が開始前の状態へ戻ります。

- カメラの位置と回転
- Projection設定
- Field of ViewまたはOrthographic Size
- マウスカーソルのロックと表示状態

ステージ境界ColliderとUIはカメラから独立しており、それぞれ`F7`と`F8`で切り替えます。

### 三人称カメラ

`F6`で開始します。滑らかに追従する注視点を中心にカメラを回転させ、マウスホイールで撮影距離を2～60の範囲で変更できます。素早く回転してもカメラと注視点の距離は変化しません。

### 自由カメラ

`F9`で開始します。プレイヤー操作用キーと競合しにくい矢印キーを使用して、カメラを自由に移動できます。プレイヤーの操作自体は停止しません。

### 移動制限の解除

`F7`を押すと、次のステージ境界オブジェクトに含まれるColliderを無効化します。撮影カメラを起動していない状態でも利用できます。

- `FIELD_WALL`
- `Wall_Front`
- `Wall_Back`
- `Wall_Left`
- `Wall_Right`
- `Wall_LeftCorner`
- `Wall_RightCorner`

床や通常の障害物のColliderは無効化しません。撮影モード中に境界だけを戻すこともできます。

### UIの非表示

ロード済みSceneに存在するCanvasをまとめて非表示にできます。再表示時は各Canvasが切り替え前に持っていた有効状態へ戻ります。

### 敵の撮影

通常の敵Waveとは別に、撮影用の通常敵6種類を既存スポナーのランダム生成範囲へ生成できます。`F10`では1体だけを種類順に切り替え、`F11`では6種類を重ならないようにまとめて生成します。`F12`で削除されるのは、この撮影機能から生成した敵だけです。

### プレイヤーとCollectibleの撮影調整

プレイヤー、Collectible、プレイヤーが遮蔽物に隠れた際の白いハイライトを個別に表示切り替えできます。ここでいうCollectibleは、飴、グミ、キノコ、氷など、`CollectibleObject`を使用する全種類が対象です。

Collectibleの削除は現在出現しているものをプールへ戻します。生成停止中に要求された生成は保留されず、生成再開後に発生した要求から再び生成されます。

三人称カメラの注視対象はプレイヤーと敵で切り替えられます。敵注視中は生存中の敵を左右に切り替えられ、敵が1体しかいない場合は切り替え入力を無視します。注視中の敵がいなくなった場合は、別の生存中の敵へ切り替わり、敵が残っていなければプレイヤー注視へ戻ります。

扉本体とは別に、扉中央の`VEX_Gate`エフェクトだけを表示／非表示にできます。

## 撮影用ショートカット

| 入力 | 動作 |
| --- | --- |
| `F9` | 自由カメラの開始／終了 |
| `F6` | 三人称カメラの開始／終了 |
| `F7` | ステージ境界Colliderの無効／有効切り替え |
| `F8` | UIの非表示／表示切り替え |
| `F10` | 撮影用の敵を1体生成し、次の種類へ切り替え |
| `Shift`＋`F10` | 撮影用の敵を1体生成し、前の種類へ切り替え |
| `F11` | 撮影用の通常敵6種類をランダム範囲へ一括生成 |
| `F12` | 撮影用に生成した敵をすべて削除 |
| `Ctrl`＋`1` | プレイヤーの表示／非表示切り替え |
| `Ctrl`＋`2` | すべてのCollectibleの表示／非表示切り替え |
| `Ctrl`＋`3` | 現在出現しているCollectibleをすべて削除 |
| `Ctrl`＋`4` | Collectibleの自動生成を停止／再開 |
| `Ctrl`＋`5` | プレイヤーの遮蔽ハイライトを無効／有効切り替え |
| `Ctrl`＋`6` | 三人称カメラの注視対象をプレイヤー／敵で切り替え |
| 敵注視中に`Ctrl`＋`←`／`→` | 注視する敵を前／次へ切り替え |
| `Ctrl`＋`7` | 扉中央のぐるぐるエフェクトを非表示／表示切り替え |
| マウス移動 | 撮影カメラの回転 |
| マウスホイール | 三人称時は撮影距離、自由カメラ時はFOVを変更 |
| `Shift`＋`WASD` | プレイヤーの移動速度と歩行アニメーション速度を2倍にする |

### 自由カメラ操作

| 入力 | 動作 |
| --- | --- |
| `↑`／`↓` | 前進／後退 |
| `←`／`→` | 左右移動 |
| `PageUp`／`PageDown` | 上昇／下降 |
| `Shift` | 押している間だけ高速移動 |

## ビルド対応したデバッグショートカット

このブランチでは、次のデバッグ操作をUnity EditorだけでなくWindows Playerビルドでも利用できます。

| 入力 | 動作 | 条件・補足 |
| --- | --- | --- |
| `F1` | 現在の生存敵を全滅させる | 通常のダメージ経路を使用 |
| `F2` | 次のStageへ進む | リザルト表示中のみ有効 |
| `F3` | 次のWaveへ進む | バトル中のみ有効 |
| `F4` | 最終Waveへ移動する | バトル中のみ有効 |
| `R` | デバッグ経験値を追加する | `DebugOverlay`の設定値を使用 |

## Inspectorで調整できる項目

`DebugOverlay.unity`の`DebugOverlayRoot`にある`MvCaptureController`から、次の値を調整できます。

- 三人称カメラの注視点の高さ
- 初期距離、最小距離、最大距離（現在の最大値は60）
- マウス回転感度
- 三人称カメラの注視点追従補間時間
- 自由カメラの移動速度
- `Shift`使用時の速度倍率
- ズーム速度
- FOVの最小値と最大値
- カメラ上下回転角の最小値と最大値

## 注意事項

- 境界Colliderを無効化してもステージの床は拡張されません。ステージ外へ進みすぎるとプレイヤーが落下する可能性があります。
- 撮影モード中もプレイヤー操作は継続します。敵Waveは開始されません。
- ショップ、ボス登場、クリアなどのカメラ演出中に撮影モードを使用すると、撮影カメラが演出カメラより優先されます。
- `F9`または`F6`で使用中のカメラモードを終了すると、撮影開始前のカメラとカーソル状態へ復元されます。
- 境界ColliderとUIはカメラモード終了後も状態を維持します。元に戻す場合は、それぞれ`F7`と`F8`をもう一度押してください。
- このブランチではデバッグ操作が通常ビルドにも含まれるため、公開用ビルドへそのまま統合しないでください。

## 実装場所

| 対象 | パス |
| --- | --- |
| 撮影モード | `Assets/Scripts/DebugTools/MvCaptureController.cs` |
| Stageデバッグ操作 | `Assets/Scripts/DebugTools/StageDebugShortcuts.cs` |
| 経験値デバッグ操作 | `Assets/Scripts/DebugTools/_TempExpDebugger.cs` |
| 撮影機能の配置Scene | `Assets/CreatorKousien/Scenes/Gameplay/DebugOverlay.unity` |
